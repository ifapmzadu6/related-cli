interface:
  display_name: "Find Related Files"
  short_description: "Find Git co-change companion files."
  default_prompt: "When edit scope is uncertain, find historical companion files once; before a commit or PR, audit the changed set once for missed docs, tests, configs, or counterparts."
