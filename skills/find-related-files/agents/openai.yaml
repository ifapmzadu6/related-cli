interface:
  display_name: "Find Related Files"
  short_description: "Find Git co-change companion files."
  default_prompt: "Find historically related files, then reconcile them with the explicit task scope and direct search."
