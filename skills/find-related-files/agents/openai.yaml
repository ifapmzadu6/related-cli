interface:
  display_name: "Audit Related Files"
  short_description: "Audit changed sets for missing companion files."
  default_prompt: "Use $find-related-files to audit this changed set once for omitted tests, docs, configs, migrations, metadata, or platform counterparts."
